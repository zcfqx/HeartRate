<Window x:Class="HeartRateMonitor.App.Views.DevicePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:HeartRateMonitor.App.Converters"
        mc:Ignorable="d"
        Title="选择设备" Width="380" Height="520"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A2E"
        Foreground="#E0E0E0"
        ResizeMode="NoResize">

    <Window.Resources>
        <DataTemplate x:Key="DeviceItemTemplate">
            <Border Background="#00FFFFFF" CornerRadius="6" Padding="12,10" Margin="2,2"
                    Cursor="Hand" MouseLeftButtonDown="OnDeviceClicked">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 设备图标 -->
                    <Border Grid.Column="0" Background="#1A6366F1" CornerRadius="20" Width="36" Height="36"
                            Margin="0,0,12,0" VerticalAlignment="Center">
                        <Path Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4Z"
                              Fill="#6366F1" Width="18" Height="18"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <!-- 设备信息 -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding DeviceName}" FontSize="14" Foreground="White" FontWeight="Medium"/>
                        <TextBlock Text="{Binding DeviceId, StringFormat='ID: {0}'}"
                                   FontSize="10" Foreground="#4DFFFFFF" Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- 信号强度 -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock FontSize="11" Foreground="#99FFFFFF" HorizontalAlignment="Center">
                            <TextBlock.Text>
                                <Binding Path="SignalStrength" StringFormat="{}{0} dBm"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <Path Data="M2,22L12,2L22,22H2Z" Fill="#6366F1" Width="12" Height="8"
                              HorizontalAlignment="Center" Margin="0,4,0,0"
                              Opacity="{Binding SignalStrength, Converter={x:Static converters:SignalStrengthToOpacityConverter.Instance}}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Path Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4Z"
                      Fill="#6366F1" Width="24" Height="24" Margin="0,0,10,0"
                      VerticalAlignment="Center"/>
                <TextBlock Text="选择心率设备" FontSize="20" FontWeight="SemiBold" Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- 状态提示 -->
        <Border Grid.Row="1" Background="#0DFFFFFF" CornerRadius="8" Padding="12,10" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="#6366F1" Margin="0,0,8,0"
                         VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ScanButtonText, StringFormat='{}● {0}'}"
                           Foreground="#99FFFFFF" FontSize="12"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 设备列表 -->
        <Border Grid.Row="2" Background="#0DFFFFFF" CornerRadius="8" Padding="4">
            <ListBox ItemsSource="{Binding Devices}"
                     Background="Transparent" Foreground="#E0E0E0"
                     BorderThickness="0"
                     ItemTemplate="{StaticResource DeviceItemTemplate}"
                     SelectedItem="{Binding SelectedDevice}"/>
        </Border>

        <!-- 底部按钮 -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{Binding ScanButtonText}"
                    Command="{Binding ToggleScanCommand}"
                    Background="#6366F1" Foreground="White" BorderThickness="0"
                    Padding="16,10" Cursor="Hand"
                    HorizontalAlignment="Stretch"
                    FontSize="14" FontWeight="SemiBold">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#818CF8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="1" Content="关闭"
                    Click="OnCloseClick"
                    Background="#33FFFFFF" Foreground="#99FFFFFF" BorderThickness="0"
                    Padding="16,10" Margin="8,0,0,0" Cursor="Hand"
                    FontSize="14">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#44FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>
