# 心率监控软件架构设计文档

## 1. 架构概述

### 1.1 设计原则
- **单一职责原则**：每个模块只负责一个功能领域
- **依赖倒置原则**：高层模块不依赖低层模块，都依赖抽象
- **开闭原则**：对扩展开放，对修改关闭
- **低耦合高内聚**：模块间依赖最小化，内部功能紧密相关

### 1.2 架构模式
采用 **MVVM (Model-View-ViewModel)** 架构模式，结合 **分层架构** 设计：

```
┌─────────────────────────────────────────────────────────────────┐
│                        Presentation Layer                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │   Views     │  │  ViewModels │  │  Converters │            │
│  │  (XAML)     │  │  (C#)       │  │  (C#)       │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│                        Application Layer                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  Services   │  │  Use Cases  │  │  DTOs       │            │
│  │  (App)      │  │  (Business) │  │  (Transfer) │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│                        Domain Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  Models     │  │  Interfaces │  │  Enums      │            │
│  │  (Entities) │  │  (Contracts)│  │  (Types)    │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│                        Infrastructure Layer                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  BLE        │  │  Database   │  │  Logging    │            │
│  │  (Hardware) │  │  (SQLite)   │  │  (Serilog)  │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 项目结构设计

### 2.1 解决方案结构

```
HeartRateMonitor.sln
│
├── src/
│   ├── HeartRateMonitor.App/                    # WPF应用程序主项目
│   │   ├── Properties/
│   │   │   └── AssemblyInfo.cs
│   │   ├── Views/                               # XAML视图
│   │   │   ├── Windows/
│   │   │   │   ├── MainWindow.xaml              # 主窗口（透明悬浮窗）
│   │   │   │   ├── SettingsWindow.xaml          # 设置窗口
│   │   │   │   └── DevicePickerWindow.xaml      # 设备选择窗口
│   │   │   ├── UserControls/
│   │   │   │   ├── HeartRateDisplay.xaml        # 心率显示控件
│   │   │   │   ├── HeartRateChart.xaml          # 心率图表控件
│   │   │   │   ├── ConnectionStatus.xaml        # 连接状态控件
│   │   │   │   └── BatteryIndicator.xaml        # 电量指示控件
│   │   │   └── Resources/
│   │   │       ├── Themes/
│   │   │       │   ├── DarkTheme.xaml           # 暗色主题
│   │   │       │   └── LightTheme.xaml          # 亮色主题
│   │   │       ├── Styles/
│   │   │       │   ├── ButtonStyles.xaml        # 按钮样式
│   │   │       │   └── TextStyles.xaml          # 文本样式
│   │   │       └── Icons/
│   │   │           ├── HeartIcon.xaml           # 心形图标
│   │   │           └── BatteryIcon.xaml         # 电池图标
│   │   ├── ViewModels/                          # 视图模型
│   │   │   ├── MainViewModel.cs
│   │   │   ├── SettingsViewModel.cs
│   │   │   ├── DevicePickerViewModel.cs
│   │   │   └── Base/
│   │   │       └── ViewModelBase.cs
│   │   ├── Converters/                          # 值转换器
│   │   │   ├── HeartRateToColorConverter.cs
│   │   │   ├── BoolToVisibilityConverter.cs
│   │   │   └── BatteryToIconConverter.cs
│   │   ├── Commands/                            # 命令
│   │   │   └── RelayCommand.cs
│   │   ├── Helpers/                             # 辅助类
│   │   │   ├── WindowHelper.cs
│   │   │   └── AnimationHelper.cs
│   │   ├── App.xaml
│   │   ├── App.xaml.cs
│   │   └── HeartRateMonitor.App.csproj
│   │
│   ├── HeartRateMonitor.Core/                   # 核心业务逻辑
│   │   ├── Models/                              # 数据模型
│   │   │   ├── HeartRateData.cs
│   │   │   ├── BleDevice.cs
│   │   │   ├── HeartRateZone.cs
│   │   │   └── DailyReport.cs
│   │   ├── Interfaces/                          # 接口定义
│   │   │   ├── IBleService.cs
│   │   │   ├── IHeartRateService.cs
│   │   │   ├── IDataService.cs
│   │   │   ├── ISettingsService.cs
│   │   │   └── ILogger.cs
│   │   ├── Enums/                               # 枚举类型
│   │   │   ├── ConnectionState.cs
│   │   │   ├── HeartRateZoneType.cs
│   │   │   └── ThemeType.cs
│   │   ├── Exceptions/                          # 自定义异常
│   │   │   ├── BleConnectionException.cs
│   │   │   └── DeviceNotFoundException.cs
│   │   └── HeartRateMonitor.Core.csproj
│   │
│   ├── HeartRateMonitor.Services/               # 服务层实现
│   │   ├── Ble/                                 # 蓝牙服务
│   │   │   ├── BleService.cs
│   │   │   ├── BleDeviceScanner.cs
│   │   │   ├── GattClient.cs
│   │   │   └── HeartRateParser.cs
│   │   ├── HeartRate/                           # 心率服务
│   │   │   ├── HeartRateService.cs
│   │   │   ├── HeartRateCalculator.cs
│   │   │   └── HeartRateZoneClassifier.cs
│   │   ├── Data/                                # 数据服务
│   │   │   ├── DataService.cs
│   │   │   ├── HeartRateRepository.cs
│   │   │   ├── DeviceRepository.cs
│   │   │   └── CsvExporter.cs
│   │   ├── Settings/                            # 设置服务
│   │   │   ├── SettingsService.cs
│   │   │   └── AutoStartHelper.cs
│   │   └── HeartRateMonitor.Services.csproj
│   │
│   └── HeartRateMonitor.Data/                   # 数据访问层
│       ├── Database/
│       │   ├── DatabaseInitializer.cs
│       │   ├── DatabaseMigration.cs
│       │   └── DbContext.cs
│       ├── Repositories/
│       │   ├── IRepository.cs
│       │   ├── HeartRateRepository.cs
│       │   └── DeviceRepository.cs
│       ├── Entities/
│       │   ├── HeartRateRecord.cs
│       │   ├── DeviceInfo.cs
│       │   └── Setting.cs
│       └── HeartRateMonitor.Data.csproj
│
├── tests/
│   ├── HeartRateMonitor.Core.Tests/
│   ├── HeartRateMonitor.Services.Tests/
│   └── HeartRateMonitor.Data.Tests/
│
└── docs/
    ├── 心率监控软件需求文档.md
    ├── 01_架构设计文档.md
    ├── 02_界面设计文档.md
    ├── 03_接口设计文档.md
    ├── 04_数据库设计文档.md
    └── 05_BLE通信设计文档.md
```

### 2.2 项目依赖关系

```
HeartRateMonitor.App
    ├── HeartRateMonitor.Core
    │   └── (无依赖)
    ├── HeartRateMonitor.Services
    │   ├── HeartRateMonitor.Core
    │   └── HeartRateMonitor.Data
    └── HeartRateMonitor.Data
        └── HeartRateMonitor.Core
```

## 3. 核心模块设计

### 3.1 BLE通信模块

```csharp
// 接口定义
public interface IBleService
{
    // 扫描状态
    bool IsScanning { get; }
    
    // 连接状态
    ConnectionState State { get; }
    
    // 设备发现事件
    event EventHandler<BleDevice> DeviceDiscovered;
    
    // 连接状态变化事件
    event EventHandler<ConnectionState> ConnectionStateChanged;
    
    // 心率数据接收事件
    event EventHandler<HeartRateData> HeartRateReceived;
    
    // 开始扫描
    Task StartScanAsync(CancellationToken cancellationToken = default);
    
    // 停止扫描
    Task StopScanAsync();
    
    // 连接设备
    Task ConnectAsync(BleDevice device);
    
    // 断开连接
    Task DisconnectAsync();
}
```

### 3.2 心率服务模块

```csharp
// 接口定义
public interface IHeartRateService
{
    // 当前心率
    int CurrentHeartRate { get; }
    
    // 心率区间
    HeartRateZoneType CurrentZone { get; }
    
    // 心率统计
    HeartRateStatistics Statistics { get; }
    
    // 心率历史（最近5分钟）
    IReadOnlyList<HeartRateData> RecentHistory { get; }
    
    // 心率数据更新事件
    event EventHandler<HeartRateData> HeartRateUpdated;
    
    // 心率区间变化事件
    event EventHandler<HeartRateZoneType> ZoneChanged;
    
    // 心率报警事件
    event EventHandler<HeartRateAlert> AlertTriggered;
    
    // 处理接收到的心率数据
    void ProcessHeartRateData(HeartRateData data);
    
    // 重置统计数据
    void ResetStatistics();
}
```

### 3.3 数据服务模块

```csharp
// 接口定义
public interface IDataService
{
    // 保存心率记录
    Task SaveHeartRateRecordAsync(HeartRateRecord record);
    
    // 获取心率历史
    Task<IEnumerable<HeartRateRecord>> GetHeartRateHistoryAsync(
        DateTime startTime, 
        DateTime endTime);
    
    // 获取设备列表
    Task<IEnumerable<BleDevice>> GetDevicesAsync();
    
    // 保存设备信息
    Task SaveDeviceAsync(BleDevice device);
    
    // 导出数据为CSV
    Task<string> ExportToCsvAsync(DateTime date);
    
    // 生成每日报告
    Task<DailyReport> GenerateDailyReportAsync(DateTime date);
}
```

## 4. 依赖注入配置

### 4.1 App.xaml.cs

```csharp
public partial class App : Application
{
    private IServiceProvider _serviceProvider;
    
    protected override void OnStartup(StartupEventArgs e)
    {
        base.OnStartup(e);
        
        var services = new ServiceCollection();
        ConfigureServices(services);
        _serviceProvider = services.BuildServiceProvider();
        
        // 初始化数据库
        var dbInitializer = _serviceProvider.GetRequiredService<DatabaseInitializer>();
        dbInitializer.InitializeAsync().Wait();
        
        // 显示主窗口
        var mainWindow = _serviceProvider.GetRequiredService<MainWindow>();
        mainWindow.Show();
    }
    
    private void ConfigureServices(IServiceCollection services)
    {
        // 数据层
        services.AddSingleton<DbContext>();
        services.AddSingleton<IHeartRateRepository, HeartRateRepository>();
        services.AddSingleton<IDeviceRepository, DeviceRepository>();
        
        // 服务层
        services.AddSingleton<IBleService, BleService>();
        services.AddSingleton<IHeartRateService, HeartRateService>();
        services.AddSingleton<IDataService, DataService>();
        services.AddSingleton<ISettingsService, SettingsService>();
        
        // 视图模型
        services.AddTransient<MainViewModel>();
        services.AddTransient<SettingsViewModel>();
        services.AddTransient<DevicePickerViewModel>();
        
        // 视图
        services.AddTransient<MainWindow>();
        services.AddTransient<SettingsWindow>();
        services.AddTransient<DevicePickerWindow>();
        
        // 日志
        services.AddLogging(builder =>
        {
            builder.AddSerilog();
        });
    }
}
```

## 5. 事件驱动架构

### 5.1 事件总线设计

```csharp
public interface IEventBus
{
    void Subscribe<TEvent>(Action<TEvent> handler);
    void Unsubscribe<TEvent>(Action<TEvent> handler);
    void Publish<TEvent>(TEvent eventData);
}

public class EventBus : IEventBus
{
    private readonly Dictionary<Type, List<Delegate>> _handlers = new();
    
    public void Subscribe<TEvent>(Action<TEvent> handler)
    {
        var eventType = typeof(TEvent);
        if (!_handlers.ContainsKey(eventType))
            _handlers[eventType] = new List<Delegate>();
        _handlers[eventType].Add(handler);
    }
    
    public void Unsubscribe<TEvent>(Action<TEvent> handler)
    {
        var eventType = typeof(TEvent);
        if (_handlers.ContainsKey(eventType))
            _handlers[eventType].Remove(handler);
    }
    
    public void Publish<TEvent>(TEvent eventData)
    {
        var eventType = typeof(TEvent);
        if (_handlers.ContainsKey(eventType))
        {
            foreach (var handler in _handlers[eventType])
            {
                ((Action<TEvent>)handler)(eventData);
            }
        }
    }
}
```

### 5.2 核心事件定义

```csharp
public class HeartRateReceivedEvent
{
    public HeartRateData Data { get; set; }
}

public class ConnectionStateChangedEvent
{
    public ConnectionState State { get; set; }
    public string DeviceId { get; set; }
}

public class HeartRateAlertEvent
{
    public int HeartRate { get; set; }
    public HeartRateAlertType AlertType { get; set; }
}

public class SettingsChangedEvent
{
    public string Key { get; set; }
    public object Value { get; set; }
}
```

## 6. 线程模型设计

### 6.1 线程策略

```
┌─────────────────────────────────────────────────────────────────┐
│                      UI Thread (Main)                           │
│  - 界面渲染                                                     │
│  - 用户交互处理                                                 │
│  - 动画执行                                                     │
├─────────────────────────────────────────────────────────────────┤
│                      BLE Thread                                 │
│  - BLE设备扫描                                                  │
│  - GATT连接管理                                                 │
│  - 数据接收                                                     │
├─────────────────────────────────────────────────────────────────┤
│                      Background Thread                          │
│  - 数据库操作                                                   │
│  - CSV导出                                                      │
│  - 统计计算                                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 线程同步

```csharp
public class HeartRateService : IHeartRateService
{
    private readonly object _lock = new object();
    private readonly ConcurrentQueue<HeartRateData> _dataQueue = new();
    
    public void ProcessHeartRateData(HeartRateData data)
    {
        // 添加到队列
        _dataQueue.Enqueue(data);
        
        // 在UI线程更新
        Application.Current.Dispatcher.Invoke(() =>
        {
            lock (_lock)
            {
                // 更新当前心率
                CurrentHeartRate = data.HeartRate;
                
                // 更新统计
                UpdateStatistics(data);
                
                // 触发事件
                HeartRateUpdated?.Invoke(this, data);
            }
        });
    }
}
```

## 7. 错误处理设计

### 7.1 异常层次结构

```csharp
public class AppException : Exception
{
    public AppException(string message) : base(message) { }
    public AppException(string message, Exception inner) : base(message, inner) { }
}

public class BleConnectionException : AppException
{
    public string DeviceId { get; }
    public BleConnectionException(string deviceId, string message) 
        : base(message)
    {
        DeviceId = deviceId;
    }
}

public class DeviceNotFoundException : AppException
{
    public DeviceNotFoundException(string deviceId) 
        : base($"Device not found: {deviceId}") { }
}

public class DataAccessException : AppException
{
    public DataAccessException(string message, Exception inner) 
        : base(message, inner) { }
}
```

### 7.2 错误处理策略

```csharp
public class ErrorHandler
{
    private readonly ILogger _logger;
    
    public void Handle(Exception ex)
    {
        _logger.Error(ex, "An error occurred");
        
        switch (ex)
        {
            case BleConnectionException bleEx:
                HandleBleError(bleEx);
                break;
            case DeviceNotFoundException devEx:
                HandleDeviceError(devEx);
                break;
            case DataAccessException dataEx:
                HandleDataError(dataEx);
                break;
            default:
                HandleGenericError(ex);
                break;
        }
    }
    
    private void HandleBleError(BleConnectionException ex)
    {
        // 尝试重连
        // 显示用户提示
    }
    
    // ... 其他处理方法
}
```

## 8. 配置管理设计

### 8.1 配置结构

```json
{
  "App": {
    "Language": "zh-CN",
    "Theme": "Dark",
    "Opacity": 0.9,
    "TopMost": true,
    "AutoStart": false
  },
  "HeartRate": {
    "UpperLimit": 180,
    "LowerLimit": 40,
    "AlertEnabled": true,
    "SoundAlertEnabled": false,
    "HistoryDuration": 300
  },
  "Chart": {
    "Visible": true,
    "RefreshInterval": 1000,
    "MaxDataPoints": 300
  },
  "Ble": {
    "AutoConnect": true,
    "ScanTimeout": 30,
    "ReconnectInterval": 5
  }
}
```

## 9. 日志设计

### 9.1 日志配置

```csharp
public static class LogConfig
{
    public static void Configure()
    {
        Log.Logger = new LoggerConfiguration()
            .MinimumLevel.Debug()
            .WriteTo.File(
                path: "logs/app-.log",
                rollingInterval: RollingInterval.Day,
                retainedFileCountLimit: 7)
            .WriteTo.Console()
            .CreateLogger();
    }
}
```

### 9.2 日志使用

```csharp
public class BleService : IBleService
{
    private readonly ILogger<BleService> _logger;
    
    public async Task ConnectAsync(BleDevice device)
    {
        _logger.LogInformation("Connecting to device: {DeviceId}", device.Id);
        
        try
        {
            // 连接逻辑
            _logger.LogInformation("Connected successfully");
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Connection failed");
            throw;
        }
    }
}
```

## 10. 性能优化设计

### 10.1 数据缓冲

```csharp
public class DataBuffer<T>
{
    private readonly Queue<T> _buffer = new();
    private readonly int _maxSize;
    
    public DataBuffer(int maxSize)
    {
        _maxSize = maxSize;
    }
    
    public void Add(T item)
    {
        _buffer.Enqueue(item);
        if (_buffer.Count > _maxSize)
            _buffer.Dequeue();
    }
    
    public IReadOnlyList<T> GetAll()
    {
        return _buffer.ToList().AsReadOnly();
    }
}
```

### 10.2 UI更新优化

```csharp
public class ThrottledUpdater
{
    private DateTime _lastUpdate = DateTime.MinValue;
    private readonly TimeSpan _interval;
    
    public ThrottledUpdater(int intervalMs)
    {
        _interval = TimeSpan.FromMilliseconds(intervalMs);
    }
    
    public bool ShouldUpdate()
    {
        var now = DateTime.UtcNow;
        if (now - _lastUpdate >= _interval)
        {
            _lastUpdate = now;
            return true;
        }
        return false;
    }
}
```

## 11. 测试策略设计

### 11.1 单元测试结构

```
tests/
├── HeartRateMonitor.Core.Tests/
│   ├── Models/
│   │   └── HeartRateDataTests.cs
│   └── Enums/
│       └── HeartRateZoneTypeTests.cs
├── HeartRateMonitor.Services.Tests/
│   ├── HeartRate/
│   │   ├── HeartRateServiceTests.cs
│   │   └── HeartRateCalculatorTests.cs
│   └── Ble/
│       └── HeartRateParserTests.cs
└── HeartRateMonitor.Data.Tests/
    └── Repositories/
        └── HeartRateRepositoryTests.cs
```

### 11.2 测试示例

```csharp
[TestClass]
public class HeartRateParserTests
{
    [TestMethod]
    public void ParseHeartRate_UINT8Format_ReturnsCorrectValue()
    {
        // Arrange
        var data = new byte[] { 0x00, 0x48 }; // Flags=0, HR=72
        
        // Act
        var result = HeartRateParser.Parse(data);
        
        // Assert
        Assert.AreEqual(72, result.HeartRate);
    }
    
    [TestMethod]
    public void ParseHeartRate_UINT16Format_ReturnsCorrectValue()
    {
        // Arrange
        var data = new byte[] { 0x01, 0x48, 0x00 }; // Flags=1, HR=72
        
        // Act
        var result = HeartRateParser.Parse(data);
        
        // Assert
        Assert.AreEqual(72, result.HeartRate);
    }
}
```
